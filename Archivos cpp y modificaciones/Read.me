El comando para compilar los codigos en linux es:
  g++ -O2 'Nombre_Archivo'.cpp -o 'Nombre_archivo' -std=gnu++0x
  
  La libreria chrono de c++ a de estar instalada en el Sistema.
 
 Usar el archivo ejecuciones.sh para ejecuta todos los archivos repetidas veces para obtener un dataset en su pc.
 Con la modificacion de este archivo se puede alterar los imputs asi como el nombre de los ejecutables.
 Se recomienda llamar a los ejecutables como suus archivos .cpp sin la extension para asi evitar tener que modificar el nombre de los archivos en el script de ejecuciones.
 Se recomienda tener tambien credado un directorio llamado "archivos_resultados" en el que se almacenaran los resultados de las ejecuciones.
